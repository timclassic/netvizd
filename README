Compiling from Git
==================
These instructions are only useful if you're building from Git - normal
package builds follow the instructions from INSTALL.  Run the following
before proceeding with the INSTALL instructions:

    $ autoreconf -i
